settings:
    topics-per-page: 5
topics:
    general:
        Version:
            brief-desc: About the version of TitanChat
            full-desc: You are running TitanChat v4.0
            permission: TitanChat.version
    commands:
        Send:
            brief-desc: Sends the message to the channel
            full-desc: |
                Description: Sends the message to the specified channel as if chatting in it
                Aliases: 'send', 's'
                Usage: /titanchat <@[channel]> send [message]
indexes:
    About:
        description: Information about TitanChat
        topics:
        - Version
        